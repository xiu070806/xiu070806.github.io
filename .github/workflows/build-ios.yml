name: TAXIMET PRO iOS TEST

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: macos-14
    steps:
      - name: Test
        run: |
          echo "GitHub Actions OK"
          sw_vers
          xcodebuild -version
          node --version
